<div class="container px-5 px-sm-0">
  <div class="row">

    <%= render 'users/list', users: @users %>

    <div class="col-md-8 offset-md-1">

      <h2>Book detail</h2>
        <table class="table">
          <tbody>
            <% @books.each do |book| %>
              <tr>
                <td></td>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
                <td><%= %></td>
                <td><%= %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>

    </div>

  </div>
</div>