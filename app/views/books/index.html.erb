<div class="container px-5 px-sm-0">

<%= render 'users/list', users: @users %>

<h2>Books</h2>

<% @books.each do |book| %>

<div class="col-md-8 offset-md-1">
  <table>
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opinion</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td></td>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
      </tr>
    </tbody>
  </table>
  <div>

<% end %>

</div>